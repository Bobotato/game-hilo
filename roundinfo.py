class roundinfo:
    """A class that holds all details for the current round"""
    def init(self, faceup, facedown, bet, prediction):
        self.faceup = faceup
        self.facedown = facedown
        self.bet = bet
        self.prediction = prediction

